export const currentIndex = state => state.currentIndex; //当前的搜索
export const maxIndex = state => state.maxIndex; //最大的索引

export const music = (state) => state.music;
export const isplaying = (state) => state.isplaying;

//book
export const book = (state) => state.book;

//历史记录
export const hstStorage = (state) => state.hstStorage